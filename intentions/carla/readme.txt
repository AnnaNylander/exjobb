First create json files (simply run create_json_intentions.py and create_json_traffic.py)
then run main.py

NOTE! this assumes that the directory ../../recorded_data exists and has correct file structure.
It is also assumed that in recorded_data/intentions/manual_annotations.csv has annotations on the form

time type
HH:MM:SS direction

for example see annotations.csv in this folder.
